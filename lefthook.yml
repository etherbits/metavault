pre-commit:
  commands:
    format:
      glob: "**/*.{ts,tsx,json,html}"
      run: bun format --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    lint:
      run: bun lint .
    unit-tests:
      run: bun test tests/unit-tests
