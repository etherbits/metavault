{
  "name": "metavault-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headless": "METAVAULT_E2E_HEADLESS=1 playwright test",
    "test:unit": "bun test unit-tests"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
